<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Student Result Management System</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: "Poppins", sans-serif;
      background: url("/images/home-bg.png") no-repeat center center/cover;
      height: 100vh;
      color: white;
      display: flex;
      flex-direction: column;
      position: relative;
      opacity: 0;
      animation: fadeIn 1.5s ease-in forwards;
    }

    /* ✅ Background fade-in animation */
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    /* ✅ Overlay for dark tint */
    body::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.55);
      z-index: 0;
    }

    nav {
      width: 100%;
      padding: 20px 60px;
      background: rgba(0, 0, 0, 0.6);
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: fixed;
      top: 0;
      left: 0;
      z-index: 10;
    }

    nav .logo {
      font-size: 1.8rem;
      font-weight: 700;
      color: #fff;
      letter-spacing: 1px;
    }

    nav ul {
      list-style: none;
      display: flex;
      gap: 25px;
    }

    nav ul li a {
      text-decoration: none;
      color: #fff;
      font-weight: 500;
      padding: 8px 15px;
      border-radius: 8px;
      transition: 0.3s;
    }

    nav ul li a:hover {
      background: #7b1fa2;
    }

    .content {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 0 20px;
      z-index: 1;
      opacity: 0;
      animation: fadeUp 2s ease-in-out 0.3s forwards;
    }

    /* ✅ Smooth upward fade for text */
    @keyframes fadeUp {
      0% {
        opacity: 0;
        transform: translateY(20px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .content h1 {
      font-size: 3rem;
      font-weight: 700;
      text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.6);
      margin-bottom: 15px;
    }

    .content p {
      font-size: 1.2rem;
      max-width: 600px;
      margin-bottom: 30px;
      text-shadow: 1px 1px 6px rgba(0, 0, 0, 0.7);
    }

    .content a {
      text-decoration: none;
      background: #6a1b9a;
      color: #fff;
      padding: 12px 25px;
      border-radius: 10px;
      font-weight: 600;
      transition: 0.3s;
    }

    .content a:hover {
      background: #4a148c;
      transform: scale(1.05);
    }
  </style>
</head>
<body>
  <nav>
    <div class="logo">Student Management</div>
    <ul>
      <li><a href="/">Home</a></li>
      <li><a href="/login">Login</a></li>
      <li><a href="/faq">FAQ</a></li>
      <li><a href="/about">About Us</a></li>
    </ul>
  </nav>

  <div class="content">
    <h1>Welcome to Student Result Management System</h1>
    <p>Manage and view academic records easily for both Students and Teachers.</p>
    <a href="/login">Get Started</a>
  </div>
</body>
</html>
